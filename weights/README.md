# Model weights

The final trained Edge-aware Residual U-Net checkpoint is not yet available in this repository.

Do not replace this placeholder with the earlier `best_model.pth` from Drive: that checkpoint was verified to be the older Simple CNN and is not the 27.946 dB / 0.74879 SSIM U-Net.

After final GPU training, save the verified checkpoint as:

`weights/final_model.pth`
