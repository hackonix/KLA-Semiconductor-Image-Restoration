# Restored test outputs

This directory will contain the actual restored images generated by the final trained model.

No fabricated or placeholder benchmark outputs are included. Generate them with:

```bash
python inference.py --input_dir /path/to/test_images --output_dir restored_outputs --weights weights/final_model.pth
```
